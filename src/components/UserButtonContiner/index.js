import {
    Container
} from './styles'

const UserButtonContainer = ({ children }) => {
    return (
        <Container>
            {children}
        </Container>
    )
}

export default UserButtonContainer